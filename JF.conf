# Docker Compose project name (used to isolate containers)
compose_project_name=monitoring-service

# Prometheus port
prometheus_port=9090

# Alertmanager port
alertmanager_port=9093

# Logs volume mapping (leave blank if not needed)
# Format: -v /host/path:/container/path
logs=

